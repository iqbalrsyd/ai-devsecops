feat(pipeline): modular-monolith support + 3-domain scope + JWT 1h + sast cleanup

Major changes (skripsi Bab 3 revisi 3-domain & 2-architecture):

BACKEND (Go):
- Add NodeIO jsonb column to pipelines table; persist per-node I/O trace
  (input keys, output diff, duration_ms, status, error) for Tahap 1-3
- Add NodeIO field to AI service GenerateResponse struct
- model: pipelinerow now carries the full execution trace for the
  PipelineDetail page (mirrors the Tahap 4 cards on RunDetail)

AI SERVICE (Python):
- domain_detection_node: limit to e-commerce | blog | iot (3-domain)
  (replaces healthcare/fintech/education); prompts + heuristics
- architecture_detection_node: limit to monolithic | modular_monolith
  (2-architecture); normalise frontend_backend alias
- coverage_library: drop healthcare_security, fintech_security,
  education_security, microservice_security, csp_security from
  coverages + type_to_coverage
- pipeline_augmentation_node: same cleanup; default augmentations
  only for the 3 supported domains
- job_reasoning_node: domain template designs (3 per supported
  domain) + AI prompt rev (extended OWASP API + CWE coverage)
- pattern_inference_node + LLM config: TIER1_RULE_IDS + RECOMMENDED_COVERAGE
  so LLM does not duplicate static rule set; MAX_RULES_PER_DOMAIN
  bumped 5 -> 8
- semgrep_rules: expand 4 files with new rules (owasp-api.yml 12 -> 26,
  ecommerce.yml 16 -> 27, blog-csp.yml 5 -> 11, iot-mqtt.yml 8 -> 17).
  Domains are now back to the skripsi-approved scope; legacy
  health/fintech/education rule files removed.
- workflow_generator: simplify sast job (drop 21k-char inline heredoc,
  use --config=.semgrep folder import). Fix duplicate domain-compliance
  in file_meta.

FRONTEND (React/TS):
- API client: onSessionExpired event bus; 1-hour access token
  + clear "Sign in again" CTA on refresh failure
- AuthContext: sessionExpired state + goToLogin action
- SessionExpiredBanner: sticky amber banner with redirect CTA
- App.tsx: wire route /projects/:projectId/pipeline/:runId;
  wrap PipelineVersionDetail with ErrorBoundary
- PipelineDetail: self-sufficient (no longer depends on location.state);
  friendly empty state when no runId/repo; auto-fetches run detail
- PipelineVersionDetail: add Tahap1to3NodeIOCards grouped by phase
  with LLM prompts surfaced per node (mirrors RunDetail Tahap 4)
- NodeSpecCard: auto-open LLM cards + copy-to-clipboard + show-full
- NodeIOCard: shared component for Tahap 1-3 + Tahap 4
- lib/node-io: shared types + helpers
- PipelinesV2: Pipeline.node_io field

TESTS:
- Update test_workflow_split, test_domain_detection, test_workflow_generator
  to reflect 3-domain / 2-arch scope; sast job no longer inlines heredoc
  (verifies --config=.semgrep + --config=p/...)

This is a pre-modular-monolith audit commit. Backup: ~/download/ai-devsecops-*.zip
